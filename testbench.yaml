laravel: '@testbench'

providers:
  - Workbench\App\Providers\WorkbenchServiceProvider
  - Guanguans\LaravelExceptionNotify\ExceptionNotifyServiceProvider

dont-discover:
  - laravel/tinker
  - spatie/laravel-ray

env:
  APP_NAME: "Testbench"
  APP_KEY: base64:UZ5sDPZSB7DSLKY+DYlU8G/V1e/qW+Ag0WF03VNxiSg=
  # DB_CONNECTION: sqlite
  # DB_DATABASE: ":memory:"
  # SOAR_ENABLED: (true)
  # SOAR_SUDO_PASSWORD: "password"

migrations:
  - workbench/database/migrations/

seeders:
  - Workbench\Database\Seeders\DatabaseSeeder

workbench:
  start: '/'
  install: true
  discovers:
    config: true
    web: true
    api: false
    commands: true
    components: false
    factories: false
    views: false

  # build:
  #   - asset-publish
  #   - create-sqlite-db
  #   - db-wipe
  #   - migrate-fresh
  #   - public:link

  # assets:
  #   - laravel-assets

  # sync:
  #   - from: public
  #     to: workbench/public
  #     reverse: true
  #   - from: storage
  #     to: workbench/storage
  #     reverse: true
